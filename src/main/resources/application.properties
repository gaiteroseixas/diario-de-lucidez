# ==========================================
# CONEXAO COM O BANCO DE DADOS
# ==========================================
spring.datasource.url=${JDBC_DATABASE_URL:jdbc:postgresql://localhost:5432/diario_sonhos}
spring.datasource.username=${JDBC_DATABASE_USERNAME:postgres}
spring.datasource.password=${JDBC_DATABASE_PASSWORD:13tereza}

# ==========================================
# CONFIGURACOES DO HIBERNATE
# ==========================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ==========================================
# CONFIGURACOES DE E-MAIL
# ==========================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=diariodelucidez@gmail.com
spring.mail.password=gaqcfmmlmzurdpbi
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ==========================================
# CONFIGURACAO DE PORTA (IMPORTANTE PARA O RENDER)
# ==========================================
server.port=${PORT:8080}